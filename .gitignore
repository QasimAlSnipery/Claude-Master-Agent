# Local secrets — never commit
.env.local

# Shopify credentials — keep local only
SESSION-NOTES.md
memory/reference_shopify_romira.md
memory/reference_shopify_prestige.md
memory/feedback_shopify_api.md

# graphify output — NEVER commit, may contain extracted secrets
graphify-out/
_theme_sections/.shopify_token

# Prestige store theme backups (not part of this repo)
.prestige-work/

# Source lecture PDFs (large, copyrighted) + extraction artifacts — keep local only
surgery_pdfs/
new_batch/
pdf_text.json
pdf_issues.json
new_text.json
new_issues.json
gen_batches.json
gen_batches_min.json

# Node build artifacts for the MCQ app
medmcq-app/node_modules/
medmcq-app/dist/
